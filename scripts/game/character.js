class Character extends CharacterAnimation {
    constructor(
        spriteSheet,
        spriteHeight,
        spriteWidth,
        spriteProportion,
        xScreenPosition) {
        super(
            spriteSheet,
            spriteHeight,
            spriteWidth,
            spriteProportion,
            xScreenPosition
        );
    }

    move() {

    }
}